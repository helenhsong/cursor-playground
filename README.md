# Cursor Playground

A collection of interaction experiments involving the cursor. Each environment gives the cursor a different relationship to its surroundings: it can become an object, reveal an image, disturb a surface, or attract the attention of figures inside the frame.
